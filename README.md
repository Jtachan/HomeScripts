# Home Scripts

General scripts to be used for "home routines" as helpers or to automate some tasks.
Each script is stored within a folder referencing for the software required:

- `py3`: Python scripts, where each script might need different requirements. All requirements for each script are mentioned at the main docstring (within the script).
- `win`: Windows `.bat` scripts to be called directly through the terminal.
